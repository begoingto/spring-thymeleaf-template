stages:
  - test

test-job:
  image: docker:29.4.1
  services:
    - name: docker:29.4.1-dind
      alias: docker
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  stage: test
  script:
    - echo "Running tests..."
    - docker info
