stages:
  - test

test-job:
  services:
    - name: docker:29.4.1-dind
  variables:
    DOCKER_HOST: "unix:///var/run/docker.sock"
    DOCKER_TLS_CERTDIR: ""
  stage: test
  script:
    - echo "Running tests..."
    - docker info
