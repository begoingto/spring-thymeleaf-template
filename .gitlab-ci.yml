stages:
  - test

test-job:
  services:
    - docker:29.4.1-dind
  variables:
    DOCKER_DRIVER: overlay2
    DOCKER_TLS_CERTDIR: ""
    DOCKER_HOST: "tcp://docker:2375"
  stage: test
  script:
    - echo "Running tests..."
    - docker info
